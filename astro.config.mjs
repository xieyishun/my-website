// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // 1. 你的 GitHub 主页地址
  site: 'https://xieyishun.github.io', 
  
  // 2. 你的仓库名（必须以斜杠开头）
  base: '/my-website', 
});
