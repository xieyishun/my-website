---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PostCard from '../../components/PostCard.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { withBase } from '../../utils/url';

export async function getStaticPaths() {
  const allPosts = await getCollection('blog');
  
  // 收集所有唯一的标签
  const tags = new Set<string>();
  allPosts.forEach(post => {
    if (post.data.tags) {
      post.data.tags.forEach(tag => {
        tags.add(tag);
      });
    }
  });

  // 为每个标签创建路径
  return Array.from(tags).map(tag => {
    const tagSlug = encodeURIComponent(tag);
    return {
      params: { tag: tagSlug },
      props: { 
        tagName: tag,
        posts: allPosts.filter(post => post.data.tags?.includes(tag))
      }
    };
  });
}

interface Props {
  tagName: string;
  posts: any[];
}

const { tagName, posts } = Astro.props;

// 按日期排序文章
const sortedPosts = posts.sort((a, b) => 
  new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime()
);

---

<!doctype html>
<html lang="zh-cn">
  <head>
    <BaseHead title={`标签: ${tagName} | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
    <style>
      main {
        width: 960px;
      }
      .tag-header {
        margin-bottom: 2rem;
        padding-bottom: 1.5rem;
        border-bottom: 2px solid #e0e0e0;
      }
      .tag-title {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
      }
      .tag-description {
        color: #666;
        font-size: 1.1rem;
      }
      .posts-list {
        list-style: none;
        padding: 0;
      }
      .post-item {
        padding: 1rem 0;
        border-bottom: 1px solid #e0e0e0;
      }
      .post-item:last-child {
        border-bottom: none;
      }
      .post-title {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
      }
      .post-title a {
        text-decoration: none;
        color: inherit;
      }
      .post-title a:hover {
        color: #0066cc;
      }
      .post-meta {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        align-items: center;
        color: #666;
        font-size: 0.9rem;
      }
      .post-description {
        margin-top: 0.5rem;
        color: #555;
      }
      .post-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-top: 0.5rem;
      }
      .post-tag {
        display: inline-block;
        padding: 0.2rem 0.6rem;
        background-color: #f0f0f0;
        border-radius: 4px;
        font-size: 0.85rem;
        text-decoration: none;
        color: #333;
        transition: background-color 0.2s;
      }
      .post-tag:hover {
        background-color: #e0e0e0;
      }
      .post-tag.current {
        background-color: #0066cc;
        color: white;
      }
      .empty-state {
        text-align: center;
        padding: 3rem;
        color: #666;
      }
      .back-link {
        display: inline-block;
        margin-bottom: 1rem;
        color: #0066cc;
        text-decoration: none;
      }
      .back-link:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <a href={withBase('tags/')} class="back-link">← 返回所有标签</a>
      
      <div class="tag-header">
        <h1 class="tag-title">🏷️ {tagName}</h1>
        <p class="tag-description">共 {sortedPosts.length} 篇文章</p>
      </div>

      {sortedPosts.length > 0 ? (
        <ul class="posts-list">
          {sortedPosts.map((post) => (
            <PostCard post={post} showTags={true} currentTag={tagName} />
          ))}
        </ul>
      ) : (
        <div class="empty-state">
          <p>此标签下暂无文章</p>
        </div>
      )}
    </main>
    <Footer />
  </body>
</html>





